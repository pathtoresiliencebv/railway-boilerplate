{
  "name": "railway-medusa-boilerplate",
  "version": "1.0.0",
  "description": "Medusa e-commerce boilerplate with Railway deployment",
  "private": true,
  "scripts": {
    "dev": "cd storefront && npm run dev",
    "dev:backend": "cd backend && yarn dev",
    "dev:frontend": "cd storefront && npm run dev",
    "build": "cd storefront && npm run build",
    "start": "cd storefront && npm start",
    "start:frontend": "cd storefront && npm start",
    "install": "cd storefront && npm install --legacy-peer-deps",
    "install:all": "cd backend && yarn install && cd ../storefront && npm install --legacy-peer-deps"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
